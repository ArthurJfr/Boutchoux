<?php

namespace App\Service;
use Core\Helper\Mail;

class MailPerso extends Mail{
    //    public function sendExemple($email, $message){
    //        $content = $this->contentAssembly('app.email.exemple', array(
    //            'message' => $message,
    //        ));
    //        $mail=new Mail();
    //        $mail->sendMail($content, $email);
    //    }
}
